<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>{{ $listing->name }}</title><script src="https://cdn.tailwindcss.com"></script></head>
<body class="bg-[#050505] text-white min-h-screen"><main class="max-w-5xl mx-auto p-8"><a class="text-orange-400" href="{{ route('ecommerce.home') }}">← Back to store</a><div class="grid md:grid-cols-2 gap-10 mt-8 bg-[#121212] border border-white/10 rounded-2xl p-7"><div class="bg-black rounded-xl flex items-center justify-center min-h-80">@if($listing->image_url)<img class="max-h-80 object-contain" src="{{ asset('storage/'.$listing->image_url) }}" alt="{{ $listing->name }}">@endif</div><div><p class="text-orange-400 text-sm font-bold">CLIENT STORE LISTING</p><h1 class="text-4xl font-black mt-2">{{ $listing->name }}</h1><p class="text-gray-400 mt-4">{{ $listing->description }}</p><p class="text-3xl font-black mt-7">₱{{ number_format((float) $listing->price, 2) }}</p><p class="mt-3 {{ $listing->available_quantity ? 'text-emerald-400' : 'text-red-400' }}">{{ $listing->available_quantity }} unit(s) available from current inventory</p>@if(session('error'))<p class="text-red-400 mt-4">{{ session('error') }}</p>@endif@if($listing->available_quantity > 0)<form class="mt-7" method="post" action="{{ route('ecommerce.listings.cart', $listing) }}">@csrf<button class="bg-orange-500 hover:bg-orange-400 text-black font-bold px-6 py-3 rounded-lg">Add to cart</button></form>@endif</div></div></main></body></html>
