<x-service-desk-admin-layout title="Knowledge Base" subtitle="Publish reusable solutions so support requests can be resolved consistently." section="knowledge">
    <div class="grid gap-6 xl:grid-cols-[1fr_22rem]"><div class="rounded-[1.875rem] bg-white p-8 text-slate-950"><div class="mb-6 flex items-center justify-between gap-4"><h2 class="text-2xl font-bold">Published articles</h2><span class="rounded-full bg-blue-50 px-4 py-2 text-sm font-bold text-[#346DCB]">{{ $articles->count() }} articles</span></div><div class="space-y-4">@forelse ($articles as $article)<article class="rounded-2xl border border-slate-200 p-5"><div class="flex flex-wrap items-start justify-between gap-3"><div><h3 class="text-lg font-bold">{{ $article->title }}</h3><p class="mt-1 text-sm text-slate-500">{{ $article->category }} · {{ $article->target_module ?: 'General' }} · {{ $article->author_name }}</p></div><span class="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold text-slate-600">{{ $article->created_at->format('M j, Y') }}</span></div><p class="mt-4 whitespace-pre-line text-sm leading-6 text-slate-700">{{ $article->content ?: 'No article content was provided.' }}</p></article>@empty<div class="py-12 text-center text-slate-500">No knowledge-base articles have been published yet.</div>@endforelse</div></div>
        <aside class="h-fit rounded-[1.875rem] bg-white p-7 text-slate-950"><h2 class="text-xl font-bold">Publish an article</h2><form method="POST" action="{{ route('admin.itsm.service-desk.knowledge-base.store') }}" class="mt-5 space-y-4">@csrf<label class="block text-sm font-semibold">Title<input name="title" required value="{{ old('title') }}" class="mt-1.5 h-10 w-full rounded border border-slate-300 px-3 font-normal"></label><label class="block text-sm font-semibold">Category<input name="category" required value="{{ old('category') }}" placeholder="e.g. Account access" class="mt-1.5 h-10 w-full rounded border border-slate-300 px-3 font-normal"></label><label class="block text-sm font-semibold">Target module<input name="target_module" value="{{ old('target_module') }}" placeholder="Optional" class="mt-1.5 h-10 w-full rounded border border-slate-300 px-3 font-normal"></label><label class="block text-sm font-semibold">Author<input name="author_name" required value="{{ old('author_name', auth()->user()->name) }}" class="mt-1.5 h-10 w-full rounded border border-slate-300 px-3 font-normal"></label><label class="block text-sm font-semibold">Article content<textarea name="content" required rows="8" class="mt-1.5 w-full rounded border border-slate-300 px-3 py-2 font-normal">{{ old('content') }}</textarea></label><button type="submit" class="w-full rounded-md bg-[#346DCB] px-4 py-2.5 font-semibold text-white hover:bg-[#2554a3]">Publish article</button></form></aside>
    </div>
</x-service-desk-admin-layout>
